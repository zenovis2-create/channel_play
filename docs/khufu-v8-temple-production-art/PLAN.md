# Plan

1. Audit the source FBX in Unity and complete a source-grounded Khufu temple research brief.
2. Freeze the accepted V5-V7 sources, scene, package hashes, and the FBX source hash.
3. Run Fable plan critique on the selective-donor and mesh-combination design.
4. Build a V8-only deterministic mesh pipeline selecting the causeway threshold, front court, open hypostyle
   vocabulary, and front hero props while excluding unsupported deep-maze and pyramid-cap geometry.
5. Place the donor slice at the Temple Hub with the source east/front axis aligned to the existing causeway.
6. Hide only the superseded V5/V6 hub renderers, preserve all colliders/bindings, and record exact ownership metadata.
7. Add static, idempotence, placement, material, route-preservation, and negative-mutation gates.
8. Build the Windows player; capture participant and fixed review views plus a deliberate visual mutation.
9. Re-run V5/V7 regression and 35-second performance validation on the final scene/build state.
10. Run aggregate evidence validation, Fable final review, staged-scope negative/positive controls, and a scoped commit.

## Stop Conditions

- After the same failure twice, stop retrying and run blocker analysis.
- Reject numerical success when direct image inspection contradicts it.
- Do not expand V8 into a full-map art replacement before the Temple Hub slice passes all gates.
- Do not stage unrelated dirty-worktree files or pre-existing package, ProjectSettings, studio, memory, or pipeline changes.
