# Status

Current decision: **the bounded geometry amendment and Windows controls pass; proceed through final
review, exact staging, scoped commit, and post-commit validation**.

## Facts

- V9 is committed at `a7ba20fd24034a0c5cf115d21d8d955797abe011`.
- V4 already owns the compressed route markers and primitive interior structure.
- The current V5 `Authentic_Interior_Spine` district is a generic 24m floor, pylons, and lintel.
- The V5 Crown route bypasses the V4 interior; V10 will not rewrite that accepted objective route.
- Existing V4 captures show a readable cutaway but sparse, freestanding interior blockout.
- Harvard survey material confirms the gallery and passage relationships used by this contract.
- Unity read-only audit plus the visual-QA amendment covers 60 renderer transitions, 39 collider transitions, eight preserved
  V4 markers, 42 Crown dependencies, and zero Crown intersection.
- The baseline scene remains at SHA256
  `0af39f99386da722f228611e0482cee46f9dfba2b57b00e6a791fc7c7fe3e346`.
- Python pre-write validation passes and its five focused mutation tests pass.
- V10 static gates pass with 220 route samples, minimum enclosure `0.792`, six renderers, 70 enabled
  BoxColliders, 60 renderer transitions, and 39 collider transitions.
- Windows development build passes with zero build errors; scene SHA256 is
  `d1778ecb2edfb7e83173a893ec82f5acb8959078ec68fc714a5f1a1320e83ad2`.
- Windows normal traversal reaches `16/16` anchors over a `96.105 m` contract, traverses `95.815 m`,
  and finishes with maximum/final error `0.150/0.030 m`.
- Great Step control stops after `0.299/2.200 m` on the exact named wall. The same horizontal move
  separately records the Gallery ramp as ground contact, proving the last-hit overwrite diagnosis.
- The Windows error-metric mutation control passes at `0.750 m` against the frozen `0.4 m` threshold.
- D3D11 performance passes at 1536x1024 Ultra: 3,552 samples; frame/main/render/GPU p95
  `8.339/2.849/2.679/2.330 ms`; bounded profiler raw `36,978,740 bytes`.
- The 163-path release inventory includes all six V10 materials, their metadata, and seven older V4
  mesh assets that the final scene directly references; no other untracked GUID is referenced.
- Bindings v2 cover working source bytes and staged Git blobs for every release source/asset input.
- A dependency-closed 872-file clean-index Unity import passes all static gates and ends at the same
  scene SHA256; Unity text assets are checkout-stable at LF.
- Final V4 validation, V5 Gate 4, V5 PlayMode, and 23 focused Python tests pass.
- External Fable re-review after the independent-review corrections returns `VERDICT: ship`.

## Assumptions

- A removable sibling overlay is lower risk than changing V4 or V5 builders.
- A separate HYBRID service return can add a playable loop without asserting exact Well Shaft form.

## Risks

- A compressed gallery can preserve count relationships while still looking visually wrong.
- New art could make the pyramid read hollow if section mass and framing are not preserved.
- The integrated cutaway remains a structurally readable production blockout, not photoreal final art.

## In Progress

- Final review-required binding refresh, deterministic staged-index receipt, scoped commit, and exact
  post-commit aggregate validation.

## Deferred

- Queen's Chamber production art, Royal Chamber Circuit, Subterranean Threshold, scan anomalies,
  global lighting, VFX, audio, and fresh-player usability study.
