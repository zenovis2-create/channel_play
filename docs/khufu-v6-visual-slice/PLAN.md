# Plan

1. **Scope and freeze**: bind V5 gameplay/topology/package/graphics inputs before editing.
2. **Research and review**: separate known Khufu complex relationships from fictional maze design;
   obtain Fable plan critique.
3. **Build the overlay**: generate Built-in RP textures/materials, assign surfaces, and add a bounded
   Temple Hub colonnade without gameplay colliders.
4. **Nearest-surface verification**: run Unity validation, idempotence, fixed-camera visual delta,
   V5 Gate 4, and CharacterController PlayMode traversal.
5. **Player verification**: create a one-scene Windows Development Player and run the visible
   1536x1024 Ultra/D3D11 performance probe.
6. **Fail-closed aggregation**: bind scene, source, capture, build, and performance hashes; exercise
   mutation tests.
7. **Final gate**: request Fable final review, apply supported blockers, rerun the aggregate validator,
   and commit only the V6 whitelist.

Retry policy: after the same failure twice, stop repeating the path and switch to blocker analysis.
The Unity MCP registration blocker reached that threshold; batch and player verification became the
proof path instead.
