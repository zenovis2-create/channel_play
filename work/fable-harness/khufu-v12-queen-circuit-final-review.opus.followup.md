Note: `Write`/`ExitPlanMode` are not available in this context, so this follow-up review is delivered inline rather than as a plan file. All checks below are read-only; I could not execute the validator, so tooling claims are assessed by reading code, tests, and receipts, and by independently re-deriving file inventories.

## 1. Findings

**F1 — The release gate exists and is fail-closed on content.** `tools/validate_khufu_v12_release.py` requires 81 non-empty files before any other check (`validate:663-670`), then enforces: exact single-line pass tokens via `require_exact_token` (`^token\r?$`, count must be 1); the pinned scene SHA256; a *recomputed-from-disk* generated/material signature (`generated_asset_signature:247-264`) against `33fbeb4d...`; scene↔GUID cross-reference for 14 metas (`check_scene_guid_references:581-593`); PNG IHDR 1600×1000, ≥64 KiB, unique bytes, manifest-bound; the build receipt bound to player/level/assembly plus 8 build-bound sources with `Errors / warnings: 0 / 185` and the protected-asset token; player receipts bound to the live assembly and CSV hashes with 137/15 trace rows and a same-frame-callback regex (`:895-906`); and set-equality of the 13 frozen Python failure IDs (`:782-789`). This is exactly the class of gate B1 said was missing.

**F2 — Scope closure is real, not asserted.** I re-derived the in-scope trees: `docs/khufu-v12-queen-circuit/` holds exactly the 9 allowlisted files; the V12 script tree holds exactly 12 sources + 12 metas, all allowlisted; the generated/material trees hold exactly the 24 files + 2 root metas that the allowlist names. Nothing in-scope is unlisted, and any stray file added to the asset trees would break `generated_asset_signature` independently of the allowlist.

**F3 — The allowlist is exact and structurally safe.** `docs/khufu-v12-queen-circuit/staging-allowlist.txt` carries 108 entries (lines 2–109). `read_allowlist:292-312` rejects backslashes, absolute paths, `..`, `.`, `//`, and duplicates. `check_staged:487` sets `permitted = allowlist - {post-commit-validation.md}`, so the post-commit report provably cannot be staged; `is_unlisted_release_scope:390-400` excludes `.log` files and `legacy-v10-raw.md`; `Builds/` appears nowhere. Each claim about the allowlist holds.

**F4 — The index gates bind bytes, not names.** `index_record`/`check_index_record` hash `git show :path`; `check_postcommit:514-565` requires `committed == inventory ∪ {2 helpers}`, `HEAD^ == base_commit`, an empty index after commit, no worktree drift on released paths, and HEAD-blob size+hash equality per record. `tools/tests/test_validate_khufu_v12_release.py` exercises this against a real temporary repository (`configure_gate_repo:29-62` runs actual `git init/add/commit`), not mocks — including `test_staged_gate_binds_exact_index_bytes`, `test_postcommit_requires_exact_inventory`, `test_staged_inventory_base_commit_is_fail_closed`, and `test_status_paths_preserves_both_sides_of_rename_records`. Test arithmetic reconciles: 16 functions with one 3-case parametrize = 18 release + 10 prewrite = the receipted `28 passed`.

**F5 — The clean-index receipt is coherent, and its 98-vs-108 gap is benign.** `runs/khufu-v12-queen-circuit/clean-index-import.md` reports 98 candidate files against a 108-path allowlist. The 10 absentees are exactly the artifacts that cannot exist at export time: the two staged helpers, the post-commit report, `release-validation.md`, `review-resolution.md`, `clean-index-import.md` itself, and the four follow-up review documents. None is hash-bound; the validator reads them only for tokens and verdicts using CRLF-tolerant regexes, so newline normalization cannot invalidate them. The receipt should name the 10 rather than leaving the delta to inference.

**F6 — `.gitattributes` is correctly scoped for this checkout, with one disclosed limit.** The four `-text` rules (`.gitattributes:9-12`) cover precisely the byte-bound JSON/receipt inputs the clean gate exposed. `.cs`, `.py`, `.txt`, `.unity` round-tripped byte-identically through the alternate index, and `validate:908-922` re-pins 8 of those disk hashes against the clean-index receipt, so a later drift fails closed. Residual: `* text=auto` means a clone under `core.autocrlf=true` would rehash `.cs`/`.md` and fail `require_hash`. Reproducibility is proven for this repository, not across machines. Incidentally, this checkout *is* `C:\Users\User\Documents\channel play` (ripgrep resolves `D:\AI2_WIN\channel_play` to it), which closes first-round item 4 — the receipts' pinned paths are this checkout.

**F7 — B2 is closed at the source, more strongly than requested.** A repository-wide grep for the three hashes matches only `tools/validate_khufu_v12_release.py` (as `RETIRED_MIGRATION_HASHES`) and `docs/khufu-v12-queen-circuit/RULES.md:43-48`. `ChannelPlayKhufuV12QueenCircuitBuilder.cs` contains no hash constant, no SHA computation, and no migration branch. `ValidateTransitionInputs:323-351` now accepts only the mutually exclusive V11-open/V12-open mesh pair and ties every proxy and V4 component state to that flag (`:339-347`); `ValidateAppliedTransitions:353-371` still re-asserts the output state on every route. The validator enforces the parity in both directions — absent from RULES.md is an error, present in the Builder is an error (`:767-775`) — so reintroduction is mechanically blocked.

**F8 — Evidence is mutually fresh.** Assembly `383c2e58...` is identical in `windows-build.md:13`, both player receipts (`:23`), and `release-validation.md:8`. `v12-final-boundary-control.md` shows `1.726 m`, overlap empty `True`, step `0.080 m`, gate `V10_PROXY_Queen_Branch_Threshold_Queen_Ownership_Gate / Sides`, and a same-frame `11008 / 11008`; `v12-final-round-trip.md` shows 15/15, 136/136, mouths `True / True`, controller `0.450 / 2.000 / 0.300 / 0.050`. The 13 IDs in `python-tests.md` are set-equal to `FROZEN_PYTHON_FAILURE_IDS`.

## 2. Open items for the procedural gates (none blocking)

**O1 — `--check-staged`/`--postcommit` do not imply `--require-reviews`** (`main:986-987` passes the flags through independently). A staged run without the flag would pass without the ship verdict and without the resolution token. The choice is disclosed in the committed report header (`Reviews required: False`), so it is auditable rather than silent, but it is not enforced. The gate command must therefore carry `--require-reviews`; the durable fix is one line: `require_reviews = args.require_reviews or args.check_staged or args.postcommit`.

**O2 — Two files are intentionally still open** and will fail `--require-reviews` as of now: `work/fable-harness/khufu-v12-queen-circuit-final-review.opus.followup.md` (the `FABLE_FINAL` path, which must exist and end in one `VERDICT: ship` line) does not exist yet, and `runs/khufu-v12-queen-circuit/review-resolution.md:14-15` deliberately withholds `KHUFU_V12_REVIEW_RESOLUTION: passed`. Both are post-verdict actions, correctly sequenced.

**O3 — Ordering constraints at staging.** `work/fable-harness/calls.jsonl` is allowlisted and dirty, so it must be staged; any harness call after staging re-dirties it and trips "staged path has an additional unstaged delta". Regenerate `release-validation.md` with `--require-reviews`, and make the harness call that writes the followup review, *before* staging.

**O4 — The staged report is self-referential but converges.** Run 1 fails only because `staged-index-validation.md` does not yet exist; add both helpers; run 2 passes and rewrites the report; re-add; run 3 is the fixed point. This terminates because `write_staged_inventory:417` excludes the helpers from the hashed records. Note that `post-commit-validation.md` is by construction never part of this commit.

## 3. Blocker status

**B1 — closed.** An exact release validator, a 108-path allowlist, a clean alternate-index import, and staged-blob/base-commit/HEAD-blob checks now exist, are tested against real Git behavior, and pass on the current tree. The staged-index and post-commit executions remain outstanding, but they are the authorized procedural gates the constraint reserves, not missing implementation or evidence.

**B2 — closed.** The three migration hashes are gone from executable code, documented as retired in RULES.md rule 19, and their reintroduction is now itself a validator failure.

No implementation or evidence blocker remains. The candidate is safe to advance to the exact staged-index and authorized-commit gates, provided those runs carry `--require-reviews` (O1) and follow the ordering in O3–O4.

VERDICT: ship

