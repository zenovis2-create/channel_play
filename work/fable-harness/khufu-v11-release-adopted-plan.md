# Khufu V11 Adopted Release Plan

## Goal

Close V11 as a scoped release on V10 commit
`42a63801ea4699c170dbe39838d63d6d93a506ec`, with visual, runtime, regression, review, and Git-boundary
proof.

## Opus Plan-Critique Resolution

| Finding | Resolution | Action |
| --- | --- | --- |
| B1 non-probative Great Step capture | adopted | Show V10 and V11 together and reframe down the open royal axis. |
| B2 disconnected integration geometry | adopted | Keep V10 visible in Integration; do not add `V4_Section_Poche` or production geometry. |
| B3 prose-only release inventory | adopted | Port the V10 allowlist, staged inventory, staged validation, and post-commit checks. |
| B4 missing legacy regression | adopted | Rerun V4/V5/V8/V9/V10 gates and classify existing drift before staging. |
| B5 static clearance insufficient | adopted | Add a real CharacterController round trip and blocker-enabled negative control. |
| B6 enclosure overclaim | adopted | Keep static enclosure as a bounded check; make exterior leaks an explicit manual-QA item. |
| B7 weak screenshot statistics | adopted | Treat statistics as integrity only; semantic acceptance remains manual and reviewer-based. |
| B8 zero renderer headroom/exporter unbound | adopted | Record 5/5 and 829/829 as accepted risk; add exporter SHA-256 to the manifest. |
| B9 capture mutation ordering | adopted | Run capture before the final idempotence gate. |
| B10 documentation drift | adopted | Reconcile negative controls and rewrite live status from final receipts. |

## Execution Order

1. Correct capture visibility/framing and manifest binding.
2. Add the V11 Windows build and CharacterController proof harness.
3. Add focused release validation, tests, and an explicit staging allowlist.
4. Run focused Python tests.
5. Rebuild/validate V11, export captures, manually inspect them, then run final idempotence and
   negative controls.
6. Build the Windows player; run normal round trip and blocker-enabled control.
7. Rerun legacy validators and write a single regression receipt.
8. Refresh status, release inventory, and all hash-bound receipts.
9. Request Opus final review.
10. Resolve findings, stage only the allowlist, validate the index, commit, and run post-commit
    validation.

V11_ADOPTED_PLAN: active
