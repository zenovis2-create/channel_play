1. Most likely cause category: **validator-geometry defect, not the proximity rule** — specifically, the synthetic renderers in `ChannelPlayCameraCutawayValidator` are being rejected by `ShouldCutaway()`'s early filters before the expanded-bounds branch is ever reached. The prime suspect is the "tiny" filter: editor-constructed test objects whose `MeshRenderer` has no mesh assigned (or whose bounds are read before a render/`Physics.SyncTransforms` pass) report `renderer.bounds` as zero-size at the transform origin, so every candidate — including both exact pylons — fails the size gate and hidden count comes back 0, which trips the scope exception. A secondary suspect in the same category: an exact-name mismatch between the synthetic objects and the two literal strings (e.g., `Pylon_-1` vs `Pylon-1`), which the exact-match candidate filter would silently reject. The `Expand(3.0f)` math itself checks out for your modeled geometry (expanded rear-pylon z-range roughly [-9.5,-4.5], y-range [-1.5,7.5] contains camera (0,7.3,-8)), so the rule logic is probably fine.

2. One diagnostic run: move the throw to after per-renderer logging (or log-then-throw). For every candidate renderer in the validator scene, log one line with exactly: `name`, `gameObject.activeInHierarchy`, `renderer.enabled`, `renderer.bounds.center`, `renderer.bounds.size`, `forceRenderingOff`, and the **first rejection reason** returned from `ShouldCutaway()` (inactive / tiny / non-candidate / expanded-contains=true|false / ray-intersect=true|false). Also log the ray origin and direction actually passed in. One run of the validator gives a complete verdict: if the two exact pylons show `bounds.size=(0,0,0)` or a name/candidate rejection, the cause is confirmed with no further experiments.

3. Minimal fix direction (only if logs confirm): if bounds are zero, give each synthetic object a real cube mesh (or a manually-set `localBounds`) so `renderer.bounds` matches the modeled center/size — do not weaken the tiny filter in runtime code, since it protects the real scene. If instead the rejection is name-based, correct the synthetic object names to the exact literals.

BLOCKER_NEXT: rerun validator with per-renderer rejection-reason logging before the throw; check exact-pylon bounds.size and rejection reason.
