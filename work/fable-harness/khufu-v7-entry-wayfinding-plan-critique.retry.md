You are Claude Fable 5 reviewing one corrected implementation decision.
Return plain text only. Do not call tools, write files, enter plan mode, or quote verdict alternatives.
Keep the answer under 500 tokens and end with exactly one standalone verdict line.

Goal:
Fix the wall-blocked Khufu first-player camera and add a bounded entry floor guide without changing
accepted V5/V6 sources or topology.

Prior blocking findings and corrections:
1. Generic `pylon` matching was too broad. Corrected rule matches only the two actual names emitted
   by the V5 builder: `V5_Valley_Gate_Pylon_-1` and `V5_Valley_Gate_Pylon_1`. No `gate`, `door`, or
   generic `pylon` substring is added.
2. Same-frame camera/cutaway evidence was ambiguous. Corrected runtime order:
   - `[DefaultExecutionOrder(100)]` on `ChannelFollowCamera`.
   - `[DefaultExecutionOrder(200)]` on `ChannelCameraOccluderCutaway`.
   - `[DefaultExecutionOrder(300)]` on the V7 entry-proof probe.
   The probe samples in `LateUpdate` after at least two settled seconds, calls `ForceRefresh`, checks
   `ActiveCutawayCount >= 1` and `DiagnosticVisibleOccluderCount == 0`, then captures at
   `WaitForEndOfFrame` so the screenshot contains the same-frame cutaway state.

Corrected ownership and budgets:
- Separate persisted V7 scene root generated by a new V7 builder after the accepted V6 rebuild.
- V5/V6 builder and validator source hashes are frozen and unchanged.
- V7 root reuses `V6_Scan_Inlay`; <=8 floor-guide renderers, <=192 vertices, <=96 triangles,
  0 colliders, 0 lights, 0 new materials, shadows off.
- Baseline full map: 795 renderers / 23,776 vertices / 16,508 triangles / 441 colliders.
- Frozen V6 root: 11 renderers / 520 vertices / 404 triangles / 0 colliders. V7 validator checks
  these separately and does not falsely claim the historical V6 validator passes downstream V7.
- Floor-guide pixel delta is advisory. Runtime occluder counts plus direct screenshot inspection are
  blocking.
- If V7 overlay invariance or performance fails, remove the overlay and ship camera hardening only.

Required gates:
- Extended editor cutaway validator for both exact Valley Gate pylon names and restore behavior.
- V7 two-run semantic idempotence and off-route-guide mutation failure.
- Rendered D3D11 entry proof bound to scene/player hashes and a blocked-pylon mutation failure.
- Fresh V5 Gate 4 and CharacterController PlayMode regression.
- Fresh 35-second visible Windows performance budget.
- Aggregate validator, external final review, staged-index verification, scoped commit.

Decision needed:
Does this corrected plan close the two blockers and remain the smallest safe V7 scope?

Give at most two remaining blocking findings. End with exactly one line:
PLAN_VERDICT: accept
Use `revise` instead only if a concrete blocker remains.
