Write

{
  "file_path": "C:\\Users\\User\\.claude\\plans\\you-are-a-scarce-declarative-kazoo.md",
  "content": "# Fable review: Khufu V10 Queen-branch capture blocker\n\n## VERDICT: proceed\n\nProceed with the proposed Codex path (diagnostic isolated render, option C), with one cheaper\nconfirming check added first. Do not spend further iterations on lighting (A) and do not open the\nfrozen manifest (D) yet.\n\n## Root cause candidates (ranked)\n\n1. **Near-field occluder filling the frame.** Evidence: same global lighting yields a readable\n   ascending-route view but a dark flat dominant surface on the Queen view; stddev 0.032 with zero\n   clipping is the signature of one close surface, not underexposure. The occluder is either\n   (a) a legacy/non-V10 renderer that should have been transitioned, or (b) V10's own junction\n   geometry.\n2. **Camera pose/near-plane error.** \"On the Queen branch centerline\" is position only; if the\n   branch inclines or the junction wall is thick, the camera may sit inside a slab or face into\n   floor/ceiling, producing the identical signature. This is indistinguishable from (1b) by pixel\n   stats alone.\n3. **Unlit/dark material on Queen-branch surfaces** — less likely (range 0.29 shows some shading\n   variation), keep as fallback only if the isolated render is equally bad.\n\n## Blocking findings\n\n- The byte/variance gate cannot distinguish occlusion from lighting; two iterations were spent\n  re-tuning lights after the ascending view already exonerated global lighting. Stop tuning\n  lighting until the occluder question is answered.\n- Camera pose is unverified. Codex-side check: log the Queen camera's world position, forward\n  vector, near-clip distance, and distance to the first V10 surface along forward before any\n  further captures.\n\n## Next single diagnostic action\n\nBefore (or alongside) the isolated render, run a **frame-space occluder query**: from the Queen\ncamera, intersect a forward ray against `Renderer.bounds` of all active renderers (bounds, not\nphysics raycast, so collider-less legacy meshes are caught) and log the nearest hits by name.\nThis names the offending renderer(s) directly — the exact enumeration a manifest amendment would\nrequire anyway — for near-zero cost. Then run the proposed isolated V10-only render to confirm\nvisually. Compare:\n\n- Isolated render also dark/flat → problem is V10 geometry or camera pose; fix camera/geometry,\n  manifest stays frozen.\n- Isolated render readable, integrated dark → legacy overlap confirmed; proceed to contract\n  amendment (below).\n\n## Conditions that justify changing the frozen manifest\n\nAll three must hold:\n\n1. The isolated V10-only render of the same Queen camera is readable and semantically correct.\n2. The occluding renderers are enumerated by exact name/path and are non-V10 legacy objects not\n   covered by the frozen 45-renderer / 39-collider transition set.\n3. The change is recorded as an explicit contract revision — updated expected counts, re-run of\n   idempotence and mutation gates — not a silent manifest edit.\n\nIf the occluder turns out to be V10's own geometry, the manifest must not change; the fix is\ncamera placement or geometry, and the final evidence remains the normal integrated capture.\nThe isolated image stays a debug receipt only, per the proposal."
}
