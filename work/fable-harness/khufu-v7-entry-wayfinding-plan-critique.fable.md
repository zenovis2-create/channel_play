Write

```json
{
  "file_path": "C:\\Users\\User\\.claude\\plans\\you-are-a-scarce-stateful-swan.md",
  "content": "# Fable Plan Critique — Khufu V7 entry/wayfinding pass\n\n## 1. Blocking flaws / over-scope\n\n- **Bare `pylon` token in shared `ChannelCameraOccluderCutaway` is too broad.** V5 deliberately built decorative district pylons (no colliders) across the map. A name-substring rule makes every pylon a cutaway candidate wherever the camera ray crosses one, changing accepted V5/V6 visual behavior map-wide, not just at Valley Gate. This is the main reason to revise.\n- **Frame-ordering evidence gap.** The camera settles in `LateUpdate`; if the cutaway scan or the probe's ray sample runs before the camera reaches its desired offset (or in a different `LateUpdate` order), \"visible ray occluders == 0\" can be a one-frame-stale false pass. Codex-side check: verify script execution order (or explicit post-camera refresh) so the probe samples the ray from the settled camera position in the same frame as the screenshot.\n- **Screenshot hash/ROI delta vs. frozen V6 baseline is fragile** across GPU/driver nondeterminism. Treat pixel delta as advisory; gate on runtime occluder evidence (activeCutaways >= 1, rayOccluders == 0) plus direct inspection of the new screenshot.\n- **Floor-guide overlay is the first thing to drop**, not a blocker: budget (<=10 renderers, 0 colliders, 0 new materials, reuse `V6_Scan_Inlay`) is tight and the supersede-not-modify stance on the V6 validator is correct. Keep it, but if perf or invariance gates fail, ship camera-entry hardening only, per the stated fallback.\n- Everything else (freeze hashes first, separate V7 root/builder, fresh V5 gates, perf rerun because scan set and renderer count changed, aggregate/mutation binding) is proportionate. No other over-scope found.\n\n## 2. Narrowest safe candidate rule\n\nDo not add a bare `pylon` token. V7 must not mutate V6-owned objects, so a runtime marker component added by the V7 builder is out. Narrowest rule that stays inside shared code: match the **exact V6 builder name of the Valley Gate pylons** (e.g. full-name or `valleygate`-scoped prefix match such as `ValleyGate_Pylon`), added as a full-name/prefix rule rather than a generic substring token. Blast radius is exactly the two gate pylons; district decorative pylons are untouched. If the shared component only supports substring tokens, add a second, prefix-matched list rather than widening the substring list.\n\n## 3. Minimum tests proving the fix without V5/V6 regression\n\n1. Extended cutaway validator: Valley Gate pylon hide/restore (batch editor), alongside the existing ceiling-beam case.\n2. Runtime entry probe in the rendered D3D11 player: activeCutaways >= 1, camera-to-player ray occluders == 0, screenshot captured after cutaway refresh from the settled `LateUpdate` camera, all bound to scene/player hashes.\n3. Direct inspection of the new first-view screenshot for route readability (pixel delta advisory only).\n4. V7 validator invariance checks: V6 source hashes, V6-root signature, V6-root metrics (795/23,776/16,508/441) unchanged; collider count unchanged; two-run semantic idempotence.\n5. Fresh V5 Gate 4 static + PlayMode CharacterController traversal.\n6. Frozen 35-second Windows performance budget rerun (justified: scan set and renderer count changed).\n\nMutation tests should include at least: re-adding an occluding pylon (probe must fail) and moving a guide strip off-route (validator must fail).\n\n## Verdict\n\nThe plan is close to minimal but must (a) replace the generic `pylon` token with a Valley-Gate-scoped exact/prefix name rule, and (b) close the LateUpdate frame-ordering gap in the entry probe before Codex touches shared camera code.\n\nPLAN_VERDICT: revise\n"
}
```

Critique complete. The verdict is **revise**, on two grounds:

1. **Blast radius of the bare `pylon` token.** V5 built decorative pylons without colliders across many districts; a substring candidate rule in the shared `ChannelCameraOccluderCutaway` would make all of them cutaway-eligible wherever the camera ray crosses one, silently changing accepted V5/V6 presentation map-wide. The narrowest safe rule is an exact/prefix match on the V6 builder's Valley Gate pylon names (e.g. `ValleyGate_Pylon*`), since a V7-added marker component would violate V6 source invariance.

2. **Frame-ordering evidence gap in the entry probe.** The camera settles in `LateUpdate`, so "ray occluders == 0" can be one frame stale unless the probe verifiably samples the ray from the settled camera position after the cutaway refresh, in the same frame as the screenshot. Codex should verify script execution order or force an explicit post-camera refresh.

Secondary notes: treat the screenshot ROI/hash delta as advisory (GPU nondeterminism) and gate on runtime occluder evidence plus direct inspection; the floor-guide overlay is acceptable scope but is the designated first drop if perf or invariance gates fail. The rest of the plan — freeze-first, separate V7 root, superseding rather than modifying the V6 validator, fresh V5 gates, perf rerun — is proportionate. Minimum test set is listed in the plan file.

`PLAN_VERDICT: revise`

ExitPlanMode
