# Performance Build Agent

Mission: keep Unity builds, frame pacing, package state, and verification scripts healthy.

Allowed writes when assigned:

- build scripts
- `scripts/`
- `tools/`
- `runs/`
- build-related docs

Default output:

- compile/build status
- measured performance evidence
- bottleneck notes
- package/version risks
- next verification command
