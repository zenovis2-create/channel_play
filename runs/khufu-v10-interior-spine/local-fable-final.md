# Khufu V10 Local Fable Final Classification

## Goal

Complete the bounded Khufu V10 interior spine and HYBRID service loop, prove the specified geometry,
movement, boundary, regression, build, performance, and release-scope contracts, then commit only the
V10 release set.

## Observable Done Surface

- Deterministic scene and mesh rebuild.
- Static, mutation, legacy, Windows traversal, boundary, error-control, build, and performance gates.
- Current-scene bindings for editor, player, and performance evidence.
- External Fable `ship`, independent read-only reviews, exact staged-index validation, scoped commit,
  and post-commit aggregate validation.

## False-Done Conditions

- A stale screenshot, binary, profile, or receipt is presented as proof of the final scene.
- The normal route cannot complete the 16-anchor round trip.
- The Great Step named boundary does not stop the controller while retaining valid ground contact.
- A mutation passes, a legacy gate regresses, or thresholds are relaxed to manufacture a pass.
- Unity package/project-setting changes or unrelated dirty files enter the V10 commit.

## Facts

- Final static validation passes with 220 clearance samples, minimum enclosure `0.792` against the
  frozen `0.750` threshold, zero collisions, six V10 renderers, and 70 enabled BoxColliders.
- The final scene SHA256 is `d1778ecb2edfb7e83173a893ec82f5acb8959078ec68fc714a5f1a1320e83ad2`.
- Windows normal traversal reaches 16/16 anchors with maximum/final error `0.150/0.030 m`.
- The Great Step control advances `0.299 m` of an attempted `2.200 m` and names the expected side
  boundary separately from the Gallery ramp ground contact.
- The `0.750 m` error mutation is rejected against the unchanged `0.4 m` threshold.
- Windows build, V4/V5 regressions, 23 focused Python tests, and the bounded profiler capture pass.
- The 163-path inventory contains every V10 material and every untracked GUID dependency referenced
  by the final scene; an isolated clean-index Unity run passes all static gates.
- Binding schemas v2 record both working release inputs and staged Git blob bytes.
- The V10 Windows build explicitly assigns the V10 scene path to `BuildPlayerOptions.scenes`.
- External Fable re-review ends with `VERDICT: ship` and no blocking findings.

## Assumptions

- The accepted contract is a historically bounded production blockout, not photoreal final art.
- `HYBRID_Service_Return` is intentionally adapted gameplay circulation and is kept distinct from
  the factual `Historic_Service_Mouth` aperture.

## Risks

- Screenshot uniqueness and semantic naming do not replace a fresh-player usability study.
- The integrated cutaway remains visually dense because earlier V4/V5 blockout structures coexist.
- Deferred chamber art, global lighting, VFX, and audio remain outside this release.

## Decision

The implementation and evidence are sufficient for a scoped V10 release candidate. Proceed only if
the independent reviews remain free of blocking findings and the live staged-index validator passes.

## Verification

Final release verification is the conjunction of the recorded Unity and Python gates, the three hash
bindings, external and local reviews, exact staged-index validation, commit-path audit, and post-commit
aggregate validation.

V10_LOCAL_FABLE: ship
