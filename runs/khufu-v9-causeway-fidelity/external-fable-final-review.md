# Khufu V9 External Fable Final Review

- Harness: `codex-fable-harness`, phase `final-review`, model `claude-fable-5`.
- Wrapper result: successful; prompt `5,154` characters; default final-review budget ceiling `$1.50`.
- Decision: **revise, then ship without re-review**.

## Blocking Findings And Resolution

1. Windows `0.000m` error needed an explicit falsifiability control.
   Resolution: the route now binds serialized anchors and minimum distance; a separate `0.750m` waypoint
   perturbation trips the `0.400m` threshold and passes its negative-control receipt.
2. The causeway needed inverse ownership protection against invisible legacy collision.
   Resolution: the full V9 renderer envelope is audited. Current inventory is `23 V9 + 2 inherited + 7
   visible-matched + 0 orphaned`; superseded renderers cannot retain solid collision except the two named floors.

## Additional Risk Reduction

- Complete BuildReport player payload binding, fresh screenshot creation, full PNG decode/pixel checks, exact staged
  whitelist, and staged/worktree equivalence were added before commit.
- Performance claims remain scoped to the documented `1536x1024 Ultra D3D11` procedure.

This receipt records Fable's actual conditional decision and the evidence-supported corrections. It does not claim
an unconditional external approval.

EXTERNAL_FABLE_REVIEW: addressed
