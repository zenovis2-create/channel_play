# Scenes

Planned scenes:

- `Boot`: startup, configuration, scene loading
- `MainMenu`: local entry point
- `Lobby`: player list and team assignment
- `School_MVP`: first playable map
- `OperatorView`: operator camera and controls
- `OverlayTest`: broadcast UI test scene
